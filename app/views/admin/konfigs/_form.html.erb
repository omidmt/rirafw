<%= form_for [:admin, @konfig] do |f| %>
  <% if @konfig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@konfig.errors.count, "error") %> prohibited this config from being saved:</h2>

      <ul>
      <% @konfig.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :key %><br />
      <%= f.text_field :key, :class => "focused"%>
    </div>

    <div class="field">
      <%= f.label :value %><br />
      <%= f.text_field :value%>
    </div>

    <div class="field">
      <%= f.label :owner %><br />
      <%= f.text_field :owner%>
    </div>

    <div class="actions">
    <%= f.submit :class => 'minibutton' %>
      <br/>
    </div>
<% end %>
<br/>
